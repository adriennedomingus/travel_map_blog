<div class="container">
  <%= render partial: "partials/photo", locals: { photo: @photo } %>
  <%= render partial: "partials/comment_form", locals: {commentable: @photo} %>
  <br>
  <div class="comments">
    <%= render partial: "partials/comments", locals: {commentable: @photo} %>
  </div>
</div>
